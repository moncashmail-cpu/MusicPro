[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "audio-synth"
version = "0.1.0"
description = "Wrapper de synthèse audio FluidSynth / SoundFonts (.sf2) et conversion MIDI vers WAV/MP3"
dependencies = [
    "pydantic>=2.0.0"
]
